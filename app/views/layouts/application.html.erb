<!DOCTYPE html>
<html>
  <head>
    <title>HorsesOnRails</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.8.0/css/bulma.min.css">
  </head>

  <body>
    <section class="section">
      <div class="container">
        <header>
          <h1 class="title is-1"><%= link_to 'Horses on Rails', root_path %></h1>
          <h2 class="subtitle is-4">Look at all the pretty horses standing in a <strong>row, row, row</strong>.</h2>
        </header>
        <%= render partial: 'horses/search' %>
        <%= yield %>
      </div>
    </section>
  </body>
</html>
